<!DOCTYPE html>
<html lang="ko" class="scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>피스웍스 | 평화를 위한 사명</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"/>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap');
    body { font-family: 'Inter', sans-serif; }
    .modal { animation: popIn 0.3s ease-out; }
    @keyframes popIn { from { opacity: 0; transform: scale(0.8); } to { opacity: 1; transform: scale(1); } }
  </style>
</head>
<body class="bg-white dark:bg-zinc-950 text-zinc-900 dark:text-white min-h-screen transition-colors duration-300">

  <div class="max-w-md mx-auto min-h-screen flex flex-col p-6 relative">

    <!-- 상단 테마 토글 -->
    <button id="theme-toggle" 
            class="absolute top-6 right-6 w-10 h-10 flex items-center justify-center text-2xl hover:scale-110 transition-transform">
      <!-- JS에서 아이콘 변경 -->
    </button>

    <!-- 프로필 영역 -->
    <div class="flex flex-col items-center pt-12 pb-8">
      <!-- 프로필 이미지 - 여기 수정! -->
      <img id="profile-img" 
           src="https://picsum.photos/id/1005/300/300" 
           alt="피스웍스 프로필" 
           class="w-32 h-32 rounded-3xl object-cover ring-4 ring-teal-200 dark:ring-teal-900 shadow-xl">
      
      <h1 class="mt-6 text-4xl font-semibold tracking-tight">피스웍스</h1>
      <p class="mt-2 text-zinc-600 dark:text-zinc-400 text-center text-lg">평화를 위한 사명</p>
    </div>

    <!-- 링크 영역 -->
    <div class="space-y-4 flex-1">
      <!-- 오픈 카카오톡방 - 여기 URL 수정! -->
      <a href="#" 
         class="block w-full bg-zinc-100 hover:bg-teal-100 dark:bg-zinc-800 dark:hover:bg-teal-950 text-center py-4 rounded-3xl text-lg font-medium transition-all hover:shadow-md active:scale-[0.98]">
        오픈 카톡방
      </a>

      <!-- Youtube - 여기 URL 수정! -->
      <a href="#" 
         class="block w-full bg-zinc-100 hover:bg-teal-100 dark:bg-zinc-800 dark:hover:bg-teal-950 text-center py-4 rounded-3xl text-lg font-medium transition-all hover:shadow-md active:scale-[0.98]">
        Youtube
      </a>

      <!-- Threads - 여기 URL 수정! -->
      <a href="#" 
         class="block w-full bg-zinc-100 hover:bg-teal-100 dark:bg-zinc-800 dark:hover:bg-teal-950 text-center py-4 rounded-3xl text-lg font-medium transition-all hover:shadow-md active:scale-[0.98]">
        Threads
      </a>

      <!-- X - 여기 URL 수정! -->
      <a href="#" 
         class="block w-full bg-zinc-100 hover:bg-teal-100 dark:bg-zinc-800 dark:hover:bg-teal-950 text-center py-4 rounded-3xl text-lg font-medium transition-all hover:shadow-md active:scale-[0.98]">
        X
      </a>
    </div>

    <!-- 문의하기 버튼 -->
    <button onclick="openModal()" 
            class="mt-8 w-full bg-teal-500 hover:bg-teal-600 text-white py-4 rounded-3xl text-lg font-medium shadow-lg shadow-teal-500/30 transition-all active:scale-[0.98]">
      문의하기
    </button>

    <!-- 푸터 -->
    <p class="text-center text-xs text-zinc-400 mt-12 pb-8">© 피스웍스 • Peace Works</p>
  </div>

  <!-- 문의하기 모달 -->
  <div id="modal" class="hidden fixed inset-0 bg-black/60 backdrop-blur-sm flex items-end md:items-center justify-center z-50">
    <div class="modal bg-white dark:bg-zinc-900 w-full max-w-md mx-4 mb-4 md:mb-0 rounded-3xl p-8 shadow-2xl">
      <div class="flex justify-between items-center mb-6">
        <h2 class="text-2xl font-semibold">문의하기</h2>
        <button onclick="closeModal()" class="text-3xl text-zinc-400 hover:text-zinc-600 dark:hover:text-zinc-300">×</button>
      </div>

      <form id="inquiry-form" class="space-y-6">
        <div>
          <label class="block text-sm font-medium mb-1.5">이름</label>
          <input type="text" id="name" required
                 class="w-full px-5 py-3.5 bg-zinc-100 dark:bg-zinc-800 rounded-2xl focus:outline-none focus:ring-2 focus:ring-teal-500">
        </div>
        <div>
          <label class="block text-sm font-medium mb-1.5">이메일</label>
          <input type="email" id="email" required
                 class="w-full px-5 py-3.5 bg-zinc-100 dark:bg-zinc-800 rounded-2xl focus:outline-none focus:ring-2 focus:ring-teal-500">
        </div>
        <div>
          <label class="block text-sm font-medium mb-1.5">문의 내용</label>
          <textarea id="message" rows="4" required
                    class="w-full px-5 py-3.5 bg-zinc-100 dark:bg-zinc-800 rounded-3xl focus:outline-none focus:ring-2 focus:ring-teal-500 resize-none"></textarea>
        </div>

        <button type="submit"
                class="w-full bg-teal-500 hover:bg-teal-600 text-white py-4 rounded-3xl font-medium text-lg transition-all active:scale-[0.98]">
          보내기
        </button>
      </form>
    </div>
  </div>

  <!-- 성공 토스트 -->
  <div id="toast" class="hidden fixed bottom-6 left-1/2 -translate-x-1/2 bg-zinc-900 text-white px-8 py-4 rounded-3xl shadow-2xl flex items-center gap-3">
    <i class="fa-solid fa-check-circle text-teal-400"></i>
    <span id="toast-text" class="font-medium">문의가 성공적으로 전송되었습니다!</span>
  </div>

<script>
  // Tailwind 초기화
  function initTailwind() {
    tailwind.config = {
      content: [],
      theme: { extend: {} },
      darkMode: 'class'
    }
  }

  // 테마 토글
  const themeToggle = document.getElementById('theme-toggle');
  function setTheme(isDark) {
    if (isDark) {
      document.documentElement.classList.add('dark');
      themeToggle.innerHTML = '☀️';
    } else {
      document.documentElement.classList.remove('dark');
      themeToggle.innerHTML = '🌙';
    }
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
  }

  themeToggle.addEventListener('click', () => {
    const isDark = !document.documentElement.classList.contains('dark');
    setTheme(isDark);
  });

  // 초기 테마 불러오기
  if (localStorage.getItem('theme') === 'dark' || 
      (!localStorage.getItem('theme') && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
    setTheme(true);
  } else {
    setTheme(false);
  }

  // 모달 열기/닫기
  function openModal() { document.getElementById('modal').classList.remove('hidden'); }
  function closeModal() { document.getElementById('modal').classList.add('hidden'); }

  // 폼 제출 (실제 연동은 여기서 바꾸시면 됩니다)
  document.getElementById('inquiry-form').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const name = document.getElementById('name').value;
    const email = document.getElementById('email').value;
    const message = document.getElementById('message').value;

    // 여기서 실제 전송 로직 넣으시면 됩니다!
    // 예: Formsubmit.co, Web3Forms, Notion API 등

    console.log('문의 내용:', { name, email, message });

    // 성공 처리
    closeModal();
    showToast();
    
    // 폼 초기화
    this.reset();
  });

  function showToast() {
    const toast = document.getElementById('toast');
    toast.classList.remove('hidden');
    setTimeout(() => {
      toast.classList.add('opacity-0', 'transition-opacity');
      setTimeout(() => {
        toast.classList.add('hidden');
        toast.classList.remove('opacity-0');
      }, 500);
    }, 3000);
  }

  // 페이지 로드 완료
  initTailwind();
</script>
</body>
</html>